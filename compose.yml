# Legacy local compose file.
#
# Do not use this file for deployment or runtime verification. The current NATS
# runtime must be validated from the active runtime endpoint and stream state,
# not from this compose profile.

name: nangman-crypto-nats-legacy-compose-disabled

services: {}
